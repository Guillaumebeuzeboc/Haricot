cmake_minimum_required(VERSION 3.5)
project(workspace)

macro(SUBDIRLIST result curdir)
      file(GLOB children RELATIVE ${curdir} ${curdir}/*)
      set(dirlist "")
      foreach(child ${children})
      		if(IS_DIRECTORY ${curdir}/${child})
            	list(APPEND dirlist ${child})
            endif()
      endforeach()
      set(${result} ${dirlist})
endmacro()

macro(BUILDDEB)
    #message("${PROJECT_NAME}")
    #message("${MAJOR_VERSION}")
    string(TOUPPER ${PROJECT_NAME} PROJECT_NAME_UPPER)
    include(${CMAKE_CURRENT_SOURCE_DIR}/debian.cmake)
    if(EXISTS "${CMAKE_ROOT}/Modules/CPack.cmake")
    include(InstallRequiredSystemLibraries)
    set(CPACK_DEB_PACKAGE_COMPONENT ON)
    set(CPACK_DEB_COMPONENT_INSTALL ON)
    set(CPACK_DEBIAN_ENABLE_COMPONENT_DEPENDS ON)
    set(CPACK_SET_DESTDIR "on")
    set(CPACK_PACKAGING_INSTALL_PREFIX "/tmp")
    set(CPACK_GENERATOR "DEB")
    set(CPACK_PACKAGE_VENDOR "${VENDOR}")
    set(CPACK_PACKAGE_CONTACT "${MAINTAINER}")
    set(CPACK_PACKAGE_VERSION_MAJOR "${MAJOR_VERSION}")
    set(CPACK_PACKAGE_VERSION_MINOR "${MINOR_VERSION}")
    set(CPACK_PACKAGE_VERSION_PATCH "${PATCH_VERSION}")
    #set(CPACK_SOURCE_PACKAGE_FILE_NAME "${PROJECT_NAME}_${MAJOR_VERSION}.${MINOR_VERSION}.${CPACK_PACKAGE_VERSION_PATCH}")

    #set (CPACK_DEBIAN_PACKAGE_SHLIBDEPS ON) # auto detect dependencies
    #set( CPACK_OUTPUT_CONFIG_FILE "${PROJECT_BINARY_DIR}/CPackConfig.cmake" )
    
    #test for components name
    set(CPACK_DEBIAN_${PROJECT_NAME_UPPER}_PACKAGE_NAME "${PROJECT_NAME}")
    set(CPACK_DEBIAN_${PROJECT_NAME_UPPER}_PACKAGE_SHLIBDEPS ON) # auto detect dependencies
    set(CPACK_COMPONENT_${PROJECT_NAME_UPPER}_DESCRIPTION "${DESCRIPTION}")
    set(CPACK_COMPONENT_${PROJECT_NAME_UPPER}_DESCRIPTION_SUMMARY "${SUMMARY}")
    set(CPACK_DEBIAN_${PROJECT_NAME_UPPER}_PACKAGE_SECTION "beuz")
    set(CPACK_DEBIAN_${PROJECT_NAME_UPPER}_FILE_NAME "${PROJECT_NAME}_${MAJOR_VERSION}.${MINOR_VERSION}.${CPACK_PACKAGE_VERSION_PATCH}")
    set(CPACK_DEBIAN_${PROJECT_NAME_UPPER}_ARCHITECTURE ${CMAKE_SYSTEM_PROCESSOR})
    set(CPACK_DEBIAN_${PROJECT_NAME_UPPER}_PACKAGE_PRIORITY "optional")
    #endtest
    #include(CPack)
    ENDIF(EXISTS "${CMAKE_ROOT}/Modules/CPack.cmake")
endmacro()

SUBDIRLIST(SUBDIRS ${CMAKE_CURRENT_SOURCE_DIR})
FOREACH(subdir ${SUBDIRS})
  ADD_SUBDIRECTORY(${subdir})
ENDFOREACH()

if(EXISTS "${CMAKE_ROOT}/Modules/CPack.cmake")
    include(CPack)
endif(EXISTS "${CMAKE_ROOT}/Modules/CPack.cmake")
